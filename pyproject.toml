[project]
name = "dart-search-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.0.2",
    "httpx>=0.28.1",
    "python-dotenv>=1.0.0",
    "click>=8.0",
]

[project.scripts]
dart = "cli:cli"
